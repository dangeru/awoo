<!DOCTYPE HTML>
<HTML>
  <head>
    <title>Awoo - Janitor Login</title>
    <link rel="stylesheet" type="text/css" href="static/dangeru.css">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="/static/favicon.ico">
    <script>
      var submit_form = function submit_form(form, route) {
        var data = new FormData(form);
        var xhr = new XMLHttpRequest();
        xhr.onreadystatechange = function() {
            if (xhr.readyState == 4) {
              if (xhr.status != 200) {
                alert("errno = -EINVAL; return;\n\n" + xhr.responseText)
              } else {
                window.location.href = "/mod_login_success"
              }
            }
        }
        xhr.open("POST", route);
        xhr.send(data);
      }
      var the_thing = function the_thing() {
        submit_form(document.getElementById("form"), "/mod");
        return false;
      }
    </script>
  </head>
  <body>
    <div id="container_login">
      <a href="javascript:location.reload();"><img src="/static/logo.png" alt="danger/u/" id="modlogo"></a><br>
      <div class="redtext">[Awoo janitor panel login]</div><br>
      <form method="POST" action="/mod" id="form" name="mod_login">
        <div class="redtext">[Username: <input type="text" name="username" id="username" placeholder="Username" class="special_styled"/>]</div>
        <div class="redtext">[Password: <input type="password" name="password" id="password" placeholder="Password" class="special_styled"/>]</div>
        <br />
        <input type="submit" name="submit" class="special_styled" id="submit" value="Send" />
      </form>
      <br />
    </div>
    <footer class="comment" style="font-size: xx-small; text-align: center;">
      <%= Config.get["footer"] %>, <a href="https://github.com/dangeru/awoo/" style="font-size: x-small;">Awoo <%= settings.awoo_version %></a>
    </footer>
  </body>
</HTML>
