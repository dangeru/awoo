<!DOCTYPE HTML>
<html>
  <head>
    <title><%= settings.config['title']%></title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>
    <h1><%= settings.config['title'] %></h1>
    <% settings.config['boards'].each do |key, array| %>
      <a href="/<%= key %>"><%= key %></a><br>
    <% end %>
    <small>Awoo <%= settings.awoo_version %></small>
  </body>
</html>
